## What changed and why

## Checklist

- [ ] Ran `node common/policies/verify-all.mjs` (add `--security` if dependencies changed)
- [ ] Updated the relevant stack README if behavior or setup steps changed
- [ ] If this touches `common/`, confirmed all three stacks (`react/`, `nextjs/`, `nodejs-backend/`) still pass
- [ ] Commit messages follow Conventional Commits
